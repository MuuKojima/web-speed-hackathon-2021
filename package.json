{
  "private": true,
  "license": "UNLICENSED",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "yarn workspaces run build",
    "gcp-build": "yarn workspaces run build",
    "start": "ls && ./server/dist/index.js"
  },
  "engines": {
    "node": "14.x.x",
    "yarn": ">=1.0.0 <2.0.0"
  }
}
