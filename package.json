{
  "private": true,
  "license": "UNLICENSED",
  "workspaces": [
    "client",
    "server"
  ],
  "scripts": {
    "build": "yarn workspaces run build",
    "gcp-build": "yarn workspaces run build",
    "start": "node /server/dist/index.js"
  },
  "engines": {
    "node": "14.x.x"
  }
}
